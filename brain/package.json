{
  "name": "clyde-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clyde's brain — Claude Agent SDK device-control agent server (runs in Termux, subscription auth).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "oneshot": "tsx src/oneshot.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.183",
    "tsx": "^4.19.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3"
  }
}
